#pragma once

namespace visos {

// Placeholder for future I/O device simulation.
// Currently, I/O completion is modeled through the BlockedQueue's wakeup_tick mechanism.
class IoDeviceSimulator {
public:
    IoDeviceSimulator() = default;
};

} // namespace visos
